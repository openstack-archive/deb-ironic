/var/log/ironic/conductor.log {
    daily
    missingok
    compress
    delaycompress
    notifempty
    postrotate
        if dpkg-vendor --derives-from ubuntu ; then
            service ironic-conductor restart >/dev/null 2>&1 || true
        else
            invoke-rc.d ironic-conductor restart >/dev/null 2>&1 || true
        fi
    endscript
}
